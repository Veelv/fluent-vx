// Fluent VX Configuration
// https://fluent-vx.dev/config

import { defineConfig } from 'fluent-vx/config';

export default defineConfig({
  // Output directory for builds
  outDir: './dist',

  // Base path for deployment
  base: '/',

  // Development server options
  server: {
    host: 'localhost',
    open: true
  },

  // Build optimizations
  build: {
    minify: 'esbuild',
    sourcemap: true
  },

  // Framework integrations
  integrations: [
    // Add integrations here
  ]
});